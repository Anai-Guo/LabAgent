# CLI Commands

LabAgent provides a command-line interface through the `labharness` command. It supports instrument scanning, measurement planning, execution, analysis, and interactive chat.

Run `labharness --help` to see all available commands and their options.
