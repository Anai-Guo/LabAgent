# MCP Tools

LabAgent exposes its functionality as an MCP (Model Context Protocol) server, allowing AI IDEs like Claude Code and Cursor to directly interact with laboratory instruments.

Start the MCP server with `labharness mcp` and configure your IDE to connect to it.
