# Terminal Panel

LabAgent offers a Claude Code-style terminal panel for interactive experiment control. The TUI provides a conversational interface where you can issue commands, ask questions about your data, and monitor measurements without leaving the terminal.

Install with `pip install "lab-agent[tui]"` and launch with `labharness panel`.
