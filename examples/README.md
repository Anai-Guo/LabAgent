# Examples

## IV Measurement Demo

A self-contained demo showing the full LabAgent workflow with simulated data:

```bash
python examples/iv_demo.py
```

This demonstrates: scan → classify → propose → validate → execute → export → analyze

No real instruments or AI API key needed.
